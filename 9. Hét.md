
`dnf install -y httpd`
`ss -ltnp` - helyi portokat listázza amik aktívak
`firewall-cmd`
`firewall-cmd --zone=internal --add-service=http`

`cat /usr/lib/firewalld/services/____.xml` - általános leírás az adott dologról (eg.: `cockpit`, `http`, `ssh`)

`firewall-cmd --runtime-to-pernament`

`systemctl start cockpit` (:9090)
