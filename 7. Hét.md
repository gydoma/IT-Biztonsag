
20G /dev/sda:
	/dev/sdb1 xfs /boot
	/dev/sdb2 xfs /
1G /dev/sdb:
	/dev/sdb1 cryptolux xfs /boot
32M /dev/sdc:
	ext2
		/keys
		/keys/home.key


dd if=/dev/random of=/keys/home.key bs=16 count=1
	egy darab 16 bájt (4\*8 hex) méretű random, home.key néven

	chmod 000 /keys/home.key

automatik felcsatolás:
	`mount /dev/sdc /keys` helyett: 
		`lsblk -f` - uuid megtalálására
		 `nano /etc/fstab` $\to$ `UUID=xxxx-xxxxx /keys ext2 defaults,nofail,ro 0 0`
			nofail - bebootol igyis-ugyis ha nincs meg az eszkoz
			ro - nincs mások általi felülírás

ezt akarjuk automatizalni:
	cryptsetup luksFormat --key-file /keys/home.key /dev/sdb1 secret
	mount /dev/mapper/secret /home
 `nano /etc/fstab` $\to$ `UUID=xxxx-xxxxx /home xfs defaults,nofail,nodev,nosuid,noexec 0 0
		 nodev - spec allomanyok ne jelenjenek meg
		 nosuid/noexec - sticky bit protection, exec fajlok letiltasa

`nano /etc/crypttab`
	`secret-home UUID=xxxx-xxxx /keys/home.key nofail`
	