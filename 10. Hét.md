exam.progcont.eu/docs

`openssl`
- aes-128-esb
- aes-128-cbc
- `genrsa -out [privkey] <bit - min 512>`
- `rsa -in [filename] -text -noout`
- `rsa -text -noout -in [pubkey] -pubin`
- `pkcs8 -topk8 -in [privkey] -out [privkey.enc]`
- `pkcs8 -topk8 -in [privkey.enc] -nocrypt`
- `pkeyutl -encrypt -inkey [pubkey] -publin -in [pubkey]`
- `pkeyutl -decrypt -inkey [privkey] -in [pubkey]`
- `pkeyutl -sign -inkey [privkey] -in text -out text.enc2`
- `pkeyutl -verifyrecover -inkey [pubkey] -pubin -in text.enc2`

- `genrsa -out [privkey] 1024`
- `rsa -in [privkey] -out [pubkey] -pubout`
- `dgst -sign [signer_privkey] -out [filename.sign] -sha256 text`
- `dgst -verify [signer_pubkey] -signature [filename.sign] -sha256 text`

Tanúsítványküldő szervezet készítése
- `trust list` (`| grep HARICA )
- `openssl genrsa -out ca.key 2048`
- `openssl req -new -key ca.key -out ca.csr`
- `openssl req -text -noout -in ca.csr`
- `openssl x509 -req -in ca.csr -out ca.crt -days 1000 -signkey ca.key`
- `openssl x509 -text -noout -in ca.crt`

- `openssl genrsa -out web.key 1024`
- `openssl req -new key web.key -out web.csr`
- `openssl x509 -req -in web.csr -out web.crt -days 100 -CA ca.crt -CAkey ca.key -CAserial ca.srl -CAcreateserial
- `sudo trust anchor ca.crt`